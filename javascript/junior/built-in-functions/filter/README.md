## `Filter` takes an array and callback function. Then returns a new array containing only those elements for which callback function returns true.

### The built-in `reduce` method is used to implement.
